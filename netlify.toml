[build]
  publish = "out"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Development settings
[dev]
  command = "npm run dev:next"
  port = 3000
  publish = "out"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200